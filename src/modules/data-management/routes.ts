import { Route } from '@angular/router';
import { DataManagementPageComponent } from '~modules/data-management/containers';

export const routes: Array<Route> = [{ path: 'data-management', component: DataManagementPageComponent }];
