import { SampleDescriptor } from './entity-descriptors/sample-descriptor.component';

describe('SampleDescriptor', () => {
	it('should create an instance', () => {
		expect(new SampleDescriptor()).toBeTruthy();
	});
});
