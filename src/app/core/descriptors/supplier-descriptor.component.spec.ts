import { SupplierDescriptor } from './supplier-descriptor.component';

describe('SupplierDescriptor', () => {
	it('should create an instance', () => {
		expect(new SupplierDescriptor()).toBeTruthy();
	});
});
