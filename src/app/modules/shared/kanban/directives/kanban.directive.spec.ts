import { KanbanDirective } from './kanban.directive';

describe('KanbanDirective', () => {
  it('should create an instance', () => {
    const directive = new KanbanDirective();
    expect(directive).toBeTruthy();
  });
});
