import { TestBed } from '@angular/core/testing';
import { AccordionModule } from './accordion.module';


describe('accordion component', () => {
	beforeEach(async () => {
		// TODO Thinh will do this
	});

	it('should display accordion-title-app', () => {
		// TODO Thinh will do this
	});

	it('should display icon angle-down when closed, angle up when open', () => {
		// TODO Thinh will do this
	});

	it('when clicking top bar (container for title and icon) it should toggle accordion', () => {
		// TODO Thinh will do this
	});

});

