image: node:9.11
stages:
  - build
  - staging
  - deploy

before_script:
  - npm install

app-build:
  stage: build
  artifacts:
    paths:
      - dist/
  script:
    - npm run build:js-training
  only:
    - master
    - production


app-staging:
  stage: staging
  image: python:latest
  dependencies:
    - app-build
  script:
    - pip install awscli
    - npm run deploy:staging
  only:
    - master

app-deploy:
  stage: deploy
  image: python:latest
  dependencies:
    - app-build
  script:
    - pip install awscli
    - npm run deploy:production
  only:
    - production